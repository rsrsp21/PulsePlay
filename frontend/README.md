# Pulse Play Frontend

This folder is the full Next.js app, including UI and API routes.

```bash
npm install
npm run dev
npm run build
```

Use `frontend/.env.local` for local secrets such as `GEMINI_API_KEY`.
